# Power Skills

## Process Management
# See all running processes with details
ps aux

# Filter processes by name
ps aux | grep chrome

# Live process monitor
top

# Kill process politely
kill <PID>

# Force kill process
kill -9 <PID>

# Kill all processes by name
killall chrome

# Run process in background
sleep 100 &
